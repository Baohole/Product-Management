include ../mixins/submenu.pug

header.header
    .cotainer 
        .row.align-items-center
            .col-3 
                .inner.logo    
                    a(href='/')
                        img(src='images/logo.sgv', alt = 'Logo')
            
            .col-9
                .inner-menu 
                    ul
                        li  
                            a(href='/') Trang chủ
                        li(class= 'sub-menu')
                            a(href='/products') Trang sản phẩm
                            +sub-menu(productCategories)
                        li  
                            a(href='/cart') Giỏ hàng(#{cart.totalQuantity ?  cart.totalQuantity : '0'})